\documentclass{article}
\usepackage{bussproofs}
\usepackage[margin=1in]{geometry}
\begin{document}

\begin{prooftree}
\AxiomC{$ x \le x\; , \; x:(p \lor q)^\circ\; , \; x:(r \to \bot)^\bullet\; , \; x:q^\circ\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:r^\circ\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:p^\circ\; , \; x:p^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:((p \lor q) \to r)^\bullet $}
\RightLabel{$ \lor^\circ $}
\UnaryInfC{$ x \le x\; , \; x:(p \lor q)^\circ\; , \; x:(r \to \bot)^\bullet\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:r^\circ\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:p^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:((p \lor q) \to r)^\bullet $}
\AxiomC{$ x \le x\; , \; x:(r \to \bot)^\bullet\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:r^\bullet\; , \; x:r^\circ\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:p^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:((p \lor q) \to r)^\bullet $}
\RightLabel{$ \to^\bullet $}
\BinaryInfC{$ x \le x\; , \; x:(r \to \bot)^\bullet\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:r^\circ\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:p^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:((p \lor q) \to r)^\bullet $}
\AxiomC{$ x \le x\; , \; x:(p \lor q)^\circ\; , \; x:(r \to \bot)^\bullet\; , \; x:q^\circ\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet\; , \; x:\bot^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:p^\circ\; , \; x:p^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:((p \lor q) \to r)^\bullet $}
\RightLabel{$ \lor^\circ $}
\UnaryInfC{$ x \le x\; , \; x:(p \lor q)^\circ\; , \; x:(r \to \bot)^\bullet\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet\; , \; x:\bot^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:p^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:((p \lor q) \to r)^\bullet $}
\AxiomC{$ x \le x\; , \; x:(r \to \bot)^\bullet\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet\; , \; x:\bot^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:r^\bullet\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:p^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:((p \lor q) \to r)^\bullet $}
\RightLabel{$ \to^\bullet $}
\BinaryInfC{$ x \le x\; , \; x:(r \to \bot)^\bullet\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:\bot^\bullet\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:p^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:((p \lor q) \to r)^\bullet $}
\RightLabel{$ \to^\bullet $}
\BinaryInfC{$ x \le x\; , \; x:(r \to \bot)^\bullet\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:p^\bullet\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:(p \lor q)^\bullet $}
\AxiomC{$ x \le x\; , \; x:(p \lor q)^\circ\; , \; x:(r \to \bot)^\bullet\; , \; x:q^\circ\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:r^\circ\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:p^\circ\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:q^\bullet $}
\RightLabel{$ \lor^\circ $}
\UnaryInfC{$ x \le x\; , \; x:(p \lor q)^\circ\; , \; x:(r \to \bot)^\bullet\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:r^\circ\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:q^\bullet $}
\AxiomC{$ x \le x\; , \; x:(r \to \bot)^\bullet\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:r^\bullet\; , \; x:r^\circ\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:q^\bullet $}
\RightLabel{$ \to^\bullet $}
\BinaryInfC{$ x \le x\; , \; x:(r \to \bot)^\bullet\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:r^\circ\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:q^\bullet $}
\AxiomC{$ x \le x\; , \; x:(p \lor q)^\circ\; , \; x:(r \to \bot)^\bullet\; , \; x:q^\circ\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet\; , \; x:\bot^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:p^\circ\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:q^\bullet $}
\RightLabel{$ \lor^\circ $}
\UnaryInfC{$ x \le x\; , \; x:(p \lor q)^\circ\; , \; x:(r \to \bot)^\bullet\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet\; , \; x:\bot^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:q^\bullet $}
\AxiomC{$ x \le x\; , \; x:(r \to \bot)^\bullet\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet\; , \; x:\bot^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:r^\bullet\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:q^\bullet $}
\RightLabel{$ \to^\bullet $}
\BinaryInfC{$ x \le x\; , \; x:(r \to \bot)^\bullet\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:\bot^\bullet\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:q^\bullet $}
\RightLabel{$ \to^\bullet $}
\BinaryInfC{$ x \le x\; , \; x:(r \to \bot)^\bullet\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:q^\bullet $}
\RightLabel{$ \lor^\bullet $}
\BinaryInfC{$ x \le x\; , \; x:(r \to \bot)^\bullet\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:(p \lor q)^\bullet $}
\RightLabel{$ \land^\bullet $}
\UnaryInfC{$ x \le x\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:(p \lor q)^\bullet $}
\RightLabel{$ \land^\bullet $}
\UnaryInfC{$ x \le x\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet $}
\RightLabel{$ \land^\bullet $}
\UnaryInfC{$ x \le x\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet $}
\end{prooftree}

\end{document}
