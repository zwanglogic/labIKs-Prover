\documentclass{article}
\usepackage{bussproofs}
\usepackage[margin=1in]{geometry}
\begin{document}

\begin{prooftree}
\AxiomC{$ x \le x\; , \; x:p^\bullet\; , \; x:q^\circ\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:p^\circ\; , \; x:(r \to \bot)^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:(p \lor q)^\circ\; , \; x:r^\circ\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet $}
\AxiomC{$ x \le x\; , \; x:p^\bullet\; , \; x:q^\circ\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:p^\circ\; , \; x:(r \to \bot)^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:(p \lor q)^\circ\; , \; x:\bot^\bullet\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet $}
\RightLabel{$ \to^\bullet $}
\BinaryInfC{$ x \le x\; , \; x:p^\bullet\; , \; x:q^\circ\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:p^\circ\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:(p \lor q)^\circ\; , \; x:(r \to \bot)^\bullet $}
\RightLabel{$ \lor^\circ $}
\UnaryInfC{$ x \le x\; , \; x:p^\bullet\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:(r \to \bot)^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:(p \lor q)^\circ\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet $}
\AxiomC{$ x \le x\; , \; x:p^\bullet\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:r^\bullet\; , \; x:r^\circ\; , \; x:(r \to \bot)^\bullet $}
\AxiomC{$ x \le x\; , \; x:p^\bullet\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:r^\bullet\; , \; x:\bot^\bullet\; , \; x:(r \to \bot)^\bullet $}
\RightLabel{$ \to^\bullet $}
\BinaryInfC{$ x \le x\; , \; x:p^\bullet\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:(r \to \bot)^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:r^\bullet\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet $}
\RightLabel{$ \to^\bullet $}
\BinaryInfC{$ x \le x\; , \; x:p^\bullet\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:(r \to \bot)^\bullet $}
\AxiomC{$ x \le x\; , \; x:q^\circ\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:p^\circ\; , \; x:(r \to \bot)^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:(p \lor q)^\circ\; , \; x:r^\circ\; , \; x:q^\bullet\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet $}
\AxiomC{$ x \le x\; , \; x:q^\circ\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:p^\circ\; , \; x:(r \to \bot)^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:(p \lor q)^\circ\; , \; x:q^\bullet\; , \; x:\bot^\bullet\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet $}
\RightLabel{$ \to^\bullet $}
\BinaryInfC{$ x \le x\; , \; x:q^\circ\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:p^\circ\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:(p \lor q)^\circ\; , \; x:q^\bullet\; , \; x:(r \to \bot)^\bullet $}
\RightLabel{$ \lor^\circ $}
\UnaryInfC{$ x \le x\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:(r \to \bot)^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:(p \lor q)^\circ\; , \; x:q^\bullet\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet $}
\AxiomC{$ x \le x\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:r^\bullet\; , \; x:r^\circ\; , \; x:q^\bullet\; , \; x:(r \to \bot)^\bullet $}
\AxiomC{$ x \le x\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:r^\bullet\; , \; x:q^\bullet\; , \; x:\bot^\bullet\; , \; x:(r \to \bot)^\bullet $}
\RightLabel{$ \to^\bullet $}
\BinaryInfC{$ x \le x\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:(r \to \bot)^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:r^\bullet\; , \; x:q^\bullet\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet $}
\RightLabel{$ \to^\bullet $}
\BinaryInfC{$ x \le x\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:q^\bullet\; , \; x:(r \to \bot)^\bullet $}
\RightLabel{$ \lor^\bullet $}
\BinaryInfC{$ x \le x\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:(r \to \bot)^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:s^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet $}
\RightLabel{$ \land^\bullet $}
\UnaryInfC{$ x \le x\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:((r \to \bot) \land s)^\bullet\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:(p \lor q)^\bullet\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet $}
\RightLabel{$ \land^\bullet $}
\UnaryInfC{$ x \le x\; , \; x:((p \lor q) \land ((r \to \bot) \land s))^\bullet\; , \; x:((p \lor q) \to r)^\bullet\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet $}
\RightLabel{$ \land^\bullet $}
\UnaryInfC{$ x \le x\; , \; x:(((p \lor q) \to r) \land ((p \lor q) \land ((r \to \bot) \land s)))^\bullet $}
\end{prooftree}

\end{document}
